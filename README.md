# Android-project
## AUTHOR
- **ФИО:** Шлегер Артём Сергеевич
- **Группа:** ИКС-432

## Структура проекта

### Файлы проекта:

#### `src/Movable.kt`
- **Интерфейс Movable** - определяет контракт для движущихся объектов
- **Свойства**: `x`, `y`, `currentSpeed`, `fullName`
- **Методы**: `move()`, `getPosition()`

#### `src/Human.kt`
- **Класс Human** - реализует интерфейс Movable
- **Движение**: Random Walk (случайное блуждание)
- **Дополнительные методы**: управление возрастом

#### `src/Driver.kt`
- **Класс Driver** - реализует интерфейс Movable
- **Движение**: прямолинейное движение
- **Особенности**: тип транспортного средства

#### `src/Main.kt`
- **Основная программа** - симуляция движения объектов
- **Использование интерфейса**: работа через `List<Movable>`
- **Параллельное выполнение**: использование Thread для одновременного движения

## Модели движения

### Random Walk (Случайное блуждание)

#### Принцип работы
Random Walk - математическая модель, где движение объекта происходит в случайных направлениях на каждом шаге. Реализована в классе `Human`.

#### Формулы движения

**Случайный угол направления:**
\[
\theta \sim U(0, 2\pi)
\]
где \(\theta\) - случайный угол равномерно распределенный от 0 до \(2\pi\)

**Новые координаты:**
\[
x_{\text{new}} = x_{\text{old}} + v \cdot \cos(\theta)
\]
\[
y_{\text{new}} = y_{\text{old}} + v \cdot \sin(\theta)
\]

**Где:**
- \(v\) - текущая скорость (`currentSpeed`)
- \(\theta\) - случайный угол направления
- \(x_{\text{old}}, y_{\text{old}}\) - предыдущие координаты
- \(x_{\text{new}}, y_{\text{new}}\) - новые координаты

### Прямолинейное движение

#### Принцип работы
Движение по прямой линии с постоянным направлением. Реализована в классе `Driver`.

#### Формулы движения

**Новые координаты:**
\[
x_{\text{new}} = x_{\text{old}} + v \cdot \cos(\theta_{\text{fixed}})
\]
\[
y_{\text{new}} = y_{\text{old}} + v \cdot \sin(\theta_{\text{fixed}})
\]

**Где:**
- \(\theta_{\text{fixed}}\) - фиксированный угол направления

## Архитектура проекта

### Принцип работы с интерфейсами
Все движущиеся объекты реализуют интерфейс `Movable`, что позволяет работать с ними единообразно через общий контракт методов и свойств.


